# Environment Variables Template
# Copy file ini menjadi .env dan isi dengan nilai yang sesuai

# Django Secret Key (Generate baru untuk production!)
# python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=your-secret-key-here

# Debug Mode (True untuk development, False untuk production)
DEBUG=True

# Allowed Hosts (pisahkan dengan koma untuk multiple hosts)
# Contoh: localhost,127.0.0.1 atau yourdomain.pythonanywhere.com
ALLOWED_HOSTS=localhost,127.0.0.1
